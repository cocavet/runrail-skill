interface:
  display_name: "RunRail"
  short_description: "Work with RunRail playbooks and workflows"
  default_prompt: "Help resolve, inspect, and execute RunRail playbooks step by step, including prompts, runs, variables, and workflow design."
